using UnityEngine;

/// <summary>
/// Marks a bathroom target and carries its point value. Added automatically by
/// PeeMinigame to each child of "bathroom Targets" (target1=+1, target2=+2, target3=+3).
/// </summary>
public class PeeTargetTag : MonoBehaviour
{
    public int points = 1;
}
